# to be done

def maximumSegmentSum(nums, removeQueries):
    # for i in range(len(nums)):
    for index in removeQueries:
        pass



print(maximumSegmentSum(nums = [1,2,5,6,1], removeQueries = [0,3,2,4,1]))